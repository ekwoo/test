package wek.test.webviewuseragenttest

import org.junit.Test

import org.junit.Assert.*

/**
 * 자동생성된 파일이다. 아직은 쓸일이 없다.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}